diff --git a/.github/workflows/ci.yml b/.github/workflows/ci.yml
index e69de29..1234567 100644
--- a/.github/workflows/ci.yml
+++ b/.github/workflows/ci.yml
@@
 on:
   push:
     branches: [main]
   pull_request:
     branches: [main]
+  schedule:           # ▶ harmonogram: codziennie 03:00 UTC
+    - cron: "0 3 * * *"

 jobs:
   build:
     runs-on: windows-latest
@@
       with:
         python-version: "3.11"
 
       - name: Set up Poetry
         uses: abatilo/actions-poetry@v2
         with:
           poetry-version: "2.1.3"
 
@@
       - name: Upload coverage to summary
         uses: actions/upload-artifact@v4
         with:
           name: coverage-xml
           path: coverage.xml
+
+      # opcjonalny e‑mail przy niepowodzeniu – aktywuj, gdy dodasz sekrety SMTP
+      # - name: Notify on failure
+      #   if: failure()
+      #   uses: dawidd6/action-send-mail@v3
+      #   with:
+      #     server_address: smtp.gmail.com
+      #     server_port: 465
+      #     username: ${{ secrets.SMTP_LOGIN }}
+      #     password: ${{ secrets.SMTP_PASS }}
+      #     subject: "FOREST 3.0 – CI failed 🚨"
+      #     to: you@example.com
+      #     from: you@example.com
+      #     body: "Sprawdź szczegóły: ${{ github.run_url }}"

diff --git a/README.md b/README.md
index 0a1b2c3..def4567 100644
--- a/README.md
+++ b/README.md
@@
+# CI Status
+![CI](https://github.com/dariuszdubis-collab/forest-3.0/actions/workflows/ci.yml/badge.svg)
+
 FOREST 3.0 – minimalny szkielet projektu…
